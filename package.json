{
  "name": "teamless-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Teamless - Beautiful PDFs with React & Tailwind CSS",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.4.0",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.13.1"
}
